{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn devhub.main:app --reload --port 8000",
    "lint": "uv run ruff check src tests && uv run ruff format --check src tests",
    "typecheck": "uv run mypy src",
    "test": "uv run pytest"
  }
}
